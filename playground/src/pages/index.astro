---
import { SomeReactComponent } from '../components/SomeReactComponent';
---

<h1>Please refer to the browser console.</h1>
<SomeReactComponent client:load />

<script>
	import { callRoute } from "typed-rest-routes/client";

	const main = async () => {
		const get = await callRoute("/api", "GET");
		const put = await callRoute("/api", "PUT", { test: "test" });
		const pos = await callRoute("/api", "POST", { test: "test" }, { id: "Name" });
		const del = await callRoute("/api", "DELETE", undefined, { id: "Name" });

		console.log({
			get,
			put,
			pos,
			del
		});
	}

	main();
</script>